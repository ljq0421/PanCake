<!-- design-system-schema: 1 -->

# 《早餐铺子》界面设计规范

## 设计目标

界面属于二维休闲模拟经营游戏。视觉应像早餐铺里的营业牌、账本、纸质订单和设备标签，而不是手机 App、后台面板或写实餐饮软件。天津章节的现有二维卡通素材是画风基准；武汉章节沿用控件结构与卡通画风，配色遵循下方“武汉城市皮肤”，并使用武汉背景与地方食物素材。

## 基础风格

- 二维、扁平、圆润、简约、粗描边、大色块、低细节。
- 主色：奶油米白、暖黄色、浅橙色、暖棕色。
- 状态色只使用少量浅绿色和浅红色，并必须同时提供文字或图标。
- 统一使用深暖棕 `#4A291C` 的 `4px` 描边；小型内部控件可使用 `3px`。
- 圆角范围为 `14～24px`，主面板优先 `22px`。
- 阴影只有一层，向下偏移约 `6px`，不叠加环境阴影。
- 单个元素控制在 2～4 个主要颜色内。

禁止玻璃拟态、透明玻璃、金属效果、真实材质、高光反射、复杂渐变、3D 按钮、浮雕、复杂花纹和写实阴影。

## 颜色令牌

| 用途 | 色值 |
|---|---|
| 奶油底色 | `#FFF1D2` |
| 柔和奶油 | `#F4DDB5` |
| 纸张白 | `#FFF8E8` |
| 营业暖黄 | `#F5B83D` |
| 强调浅橙 | `#E9873D` |
| 辅助暖棕 | `#9A592D` |
| 描边深棕 | `#4A291C` |
| 正文深棕 | `#3B241A` |
| 成功浅绿 | `#76A951` |
| 错误浅红 | `#D95D47` |

颜色与公共控件样式集中在 `Scripts/UI/TianjinUi.cs` 的 Godot `Theme` 中维护，不在单个页面建立另一套视觉语言。

## 字体与可操作性

- 不增加外部字体依赖，使用 Godot 当前可用的中文系统字体回退。
- 1920×1080 下正文不小于 `18px`；订单商品数量可降到 `14px`，但必须搭配清晰图片。
- 可交互区域不小于 `48×48px`。
- 按钮文案使用动作结果，例如“打开铺门 · 开始 Day 5”“收好收入 · 返回经营首页”。
- 禁用态降低饱和度，同时保留明确文字，不仅依赖灰色表达。

## 核心组件

### 营业按钮

暖黄色大色块、深棕粗描边、明显圆角和单层向下阴影。它必须是经营首页视觉权重最高的操作。

### 面板与营业牌

使用奶油色或纸张白，主面板圆角 `22px`。内容按“标题—关键数字—说明—操作”排列，避免数据表格式密集堆叠。

### 顾客订单气泡

使用纸张白、深棕粗描边和单层阴影。订单以食物图片为主，显示商品名称、已交付/总数量和配料小图。单卖油条必须写明“单卖油条”，与煎饼内油条配料区分。

天津使用 `StockUI/order_body.png` 九宫格底框和独立等比尾巴，耐心条底框同目录读取。装饰不拦截鼠标；数量、商品与配料继续动态排版。

### 耐心条

气泡与人物下方固定显示。绿色、棕橙、红色变化之外，人物徽标和反馈文案也要表达优先级或风险。

### 设备与食材

设备等级必须切换真实美术图片。面糊、鸡蛋、酱料、薄脆、香葱、火腿、油条和豆浆均使用对应图片；拖拽预览继续使用相同图片，禁止退回纯色文字块。

天津鸡蛋、薄脆、火腿六份以内逐份显示，超过六份用最多十个互不遮挡的固定位置表达库存疏密，取消后层重复叠图。排列由容量决定，取料和补货时仅改变可见数量，不移动或缩放剩余食材。豆浆始终逐杯显示，香葱通过互不重叠的1～3簇表达满、半、少，液面保留对应分档。天津营业工作台不显示库存数字，以素材疏密、簇数和液面表达余量，熟油条以1～3组堆叠表达少、半、满。空库存露出空盘或空碗；小料补货使用已有素材预览与进度条，完成前禁止取用。详见 `docs/天津库存与订单气泡优化.md`。

### 金币托盘与补货

天津营业工作台新增金币托盘，位于成品托盘左侧，展示当日账本收入对应的示意钱堆；无需点击收钱。六种小料和豆浆统一长按0.45秒补货，空盘也长按；短按取料、移动拖料与长按互斥。两行三列槽位收紧，煎饼炉右移50设计像素。布局、输入及结算规则详见 [天津金币托盘与长按补货](docs/天津金币托盘与长按补货.md)。

### 收据结算

采用居中的奶油色纸张覆盖层，依次强调总收入、收入构成、完成顾客、满意度、纪录/星级和解锁提示。唯一主操作为“返回经营首页”。

## 页面结构

### 经营首页

- 顶栏：店名、金币、最高解锁日、天津最高星级。
- 左侧营业牌：今日主题、历史最佳或经营提示、唯一主按钮、经营手账与城市地图次级入口。
- 右侧设备区：三级煎饼炉、油条锅和配料台的当前状态、升级效果与价格。
- 开发入口仅在 `--dev-ui` 启动参数存在时显示。

### 营业场景

- 使用 `resource/art/TianJin/早餐铺主界面-1920x1080.png` 全屏铺设，不拉伸透明素材本体。
- 顶部为精简状态条；窗口区域最多排列五名顾客及其订单。
- 左侧油条锅、中部煎饼炉、右侧配料台；右上工作台边缘为出餐区和豆浆。
- 操作错误、缺货、成功和开店提示为短时反馈，不保留持续调试文字。

### 经营手账与城市地图

天津经营手账采用居中的摊开双页账本，奶油纸面、暖棕封皮、浅橙书签，沿用天津主题。左页按三列五行呈现 Day 1～15，右页显示所选日期的主题食物或顾客图片，以及已有记录的最佳收入、满意度和 Perfect 单数。文字和按钮由程序绘制，底图不包含文字。

- 打开时默认选中最高已解锁日期；点日期只预览，右页“开始营业／再次营业 · Day N”才进入所选已解锁日期。未解锁日期可查看解锁条件，禁止开始。
- 日期使用“等待开店／已记录／未解锁”文字区分状态；选中暖黄、锁定柔和奶油，状态文字统一正文深棕 `#3B241A`，焦点以浅橙描边标示。记录印章仅表示已有营业记录，不表示每日星级；星级只在章节汇总显示。
- 无记录时显示等待开店文案，不填零值或虚构数据；存档无法读取时明确说明恢复方式并禁止营业。重置进度保留确认弹窗。
- 手账是完整模态层：遮挡层拦截背景点击，Tab／Shift+Tab 在内部循环，方向键移动日期焦点，Enter／Space 激活当前按钮，Esc 或“合上手账”关闭并恢复原焦点。重置确认期间由确认弹窗接管输入。无翻页动画。

城市地图是次级入口；Day 15 达标后突出天津印章与武汉入口。手账实现与验收详见 [天津经营手账](docs/天津经营手账.md)。

## 动效

- 只使用轻量 Tween：顾客入场淡入与轻微缩放、选中轻微放大、提示淡入上移、城市点亮。
- 动效用于状态变化和操作反馈，不做循环装饰。
- 默认时长约 `180～650ms`，不使用弹跳过度、粒子堆叠或写实物理效果。

## 资源规则

- 天津素材统一通过 `TianjinArtCatalog` 的稳定 ID 读取并缓存，禁止在刷新循环中重复加载。
- 原始素材不修改；只有 v2 背景通过项目脚本确定性裁切缩放为 1920×1080 运行版本。
- 透明大画布素材通过独立显示尺寸、锚点和点击区域处理。
- 顾客素材缺失时回退到当前男上班族图片，并用文字徽标区分普通、上班族、熟客和大订单。


## 武汉城市皮肤

武汉经营首页、营业工作台和收据使用 `WuhanUi`，以现有武汉 v3 背景及 V1.1 城市视觉规范为准。保留圆角、粗描边、布局及通用金币、成功和错误反馈语义，天津继续使用原主题。

- 纸张底色 `#F7F3E8`，次级面板 `#E9E9DC`。
- 青绿强调 `#387F70`，墨绿描边及主按钮 `#24594F`。
- 正文 `#293E36`，次级正文 `#45594F`；禁用背景 `#D5DCD2`。
- 主按钮使用米白文字，收据 RichTextLabel 显式覆盖深色正文。普通文字与背景对比度至少 4.5:1，覆盖按钮普通、悬停、按下和禁用状态。
- 订单每种商品单独一行，并显示已交付／需求数量。
- 成品从面碗、豆皮备餐盘、蛋酒成品杯直接拖给顾客，落点决定接餐人。页面无选中顾客或点击交付入口；拌匀后松手再按下开始拖拽。
