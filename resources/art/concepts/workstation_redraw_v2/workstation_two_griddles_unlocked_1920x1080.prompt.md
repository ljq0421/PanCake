# 工作台重绘 v2：双鏊解锁状态

Use case: precise-object-edit

Input image: `workstation_initial_one_griddle_1920x1080.png` is the strict composition and art-style reference.

Primary request: Change only the central jianbing zone to the unlocked two-griddle state. Preserve the 16:9 frame, shallow counter height, background, customers, cartoon hand-painted style, soy-milk equipment, youtiao equipment, all ingredient-container positions, lighting, and colors.

Central-zone change: Replace the initial single griddle with exactly two matching round griddles of equal size, side by side with even spacing and centered as a pair within the central half of the counter. The tabletop beneath and between both griddles is completely continuous: no seams, planks, joints, stripes, dividers, or panel lines.

Constraints: No third, locked, dotted, empty, or ghost griddle. No lower cabinet face, ingredient rail, storage-slot strip, text, logo, watermark, extra UI, photorealistic rendering, or layout drift.
