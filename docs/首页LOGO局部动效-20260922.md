# 首页 LOGO 动效（2026-09-22，按用户要求收敛）

只保留入场轻落回弹和蒸汽缓慢飘散。撤回整体呼吸、太阳与光芒、感叹号、食物、悬停和阴影改动；删除上一版生成的分层素材，恢复原始中英文 LOGO。

- 入场：从上方 40px 轻落，缩放 0.90 → 1.035 → 0.99 → 1，约 0.6 秒。应用会话首次显示时播放，按钮保持立即可用。
- 蒸汽：直接提取原始 PNG 的蒸汽像素，2.4 秒循环，缓慢上升、轻微横移并淡出；其余像素沿用原图。提取区域沿蒸汽与文字、叶片交界限制，不重新生成文字和装饰。
- 相接描边使用短距离透明渐变衔接，避免飘动后出现生硬的切口。
- 中英文使用同一控制器；正式版与试玩版没有实现分支。
- 弹窗、失焦、隐藏和离开首页时暂停。减少动态时显示未经拆分的原始 LOGO。

专项入口：`Scenes/Tests/StartScreenSelfTest.tscn -- --logo-motion-only --capture`，小窗口加 `--small`。测试比较拆分前后像素，检查非蒸汽区域未变、悬停不改变尺寸、暂停与恢复、减少动态和页面生命周期。验收截图使用 `logo-steam-*.png`。

本版验证：完整构建零错误、零警告；LOGO 专项 23 项通过；首页原有动效回归 41 项通过；已查看中英文实际视口以及最终 1920×1080 整页。
