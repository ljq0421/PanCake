# 铃铛素材调整

使用内置 ImageGen 编辑既有铃铛，武汉实际视口作为粗线画风参考；成品保存为 `resource/art/TianJin/SupplyCall/supply_bell.png`，两城共用。请求纯绿背景便于抠图，工具实际返回已透明 PNG；已检查角落 alpha 为 0，并在实际视口验证边缘。

最终提示词：

> Edit target first image: single gold service counter bell with red-brown base, retain silhouette and slightly top-down perspective. Second image is style reference ONLY for thick dark brown cartoon outlines and flat simple warm color fills. Redraw bell with substantially bolder uniform outer and inner contour lines (outer stroke about 4% of object width, inner about 2%), smooth shapes, 2-3 shading blocks, a single cream highlight. Remove painterly texture and tiny detail. Must remain legible as 80-pixel game sprite. Center isolated single bell fully within square canvas occupying 85% width. Solid pure chroma green #00FF00 background for later keying, no green in bell, no drop shadow, no text or scenery. Output saveable image asset.

## 两城共用粗线版本与尺寸统一

再次使用内置 ImageGen 编辑原铃铛，以天津实际视口的设备描线为风格参考，成品为 `resource/art/TianJin/SupplyCall/supply_bell_bold.png`。提示词要求保留金色铃身、红色底座、轮廓和高光，将深棕色外轮廓与内部描线加粗，使用纯绿背景抠图。工具返回透明 PNG；已检查四角 alpha 为 0，铃身内部 alpha 非零。

天津原显示框为 104×100，现按 1.2 倍改为 124.8×120；正方形素材在框内实际显示为 120×120。武汉使用同一素材和显示框，铃身也为 120×120，并保持原桌沿落点。已在两城 1920×1080 实际视口检查。
