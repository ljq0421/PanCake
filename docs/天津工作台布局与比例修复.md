# 天津工作台布局与比例修复

最新调整见 [天津金币托盘与长按补货](天津金币托盘与长按补货.md)：小料区起点、列间距、煎饼炉横向偏移、上方托盘位置和补货入口已更新；下文保留此前布局调整记录。

后续更新：盘内油条、食材余量与订单气泡的最终规则见[天津库存与订单气泡优化](天津库存与订单气泡优化.md)。下文固定三件代表图及单杯豆浆的旧规则已由该文替代；设备位置和操作热区继续有效。

日期：2026-09-08。依据当前主工作台截图、三列两排方案及用户确认的设备前移调整。

第三轮已完成：修正盘内密度、信息归属，并将两台设备连同生熟油条区向桌前平移 50 设计像素，缩小左下、中下空带。设备显示尺寸与局部操作几何保持不变。

## 最终行为

- 保留店铺画风、顾客柜台基线、三级炉子及炸锅显示尺寸；炉面、划动区和炸篮命中区域随设备同步前移 50 设计像素。
- 炉子下方的提示和操作按钮改为同一行，位于 y925～981；翻面、折叠、装袋和清理按钮出现时，提示缩至左侧，按钮位于右侧，避免越过桌沿。
- 天津营业页面启用 `PancakeWorkstation.UseServingTray`，该选项必须在节点加入场景树前配置；默认关闭以保留实验台等旧场景。
- 配料改为三列两排：左列面糊／酱料，中列薄脆／火腿，右列鸡蛋／香葱。未解锁食材隐藏后保留空位。
- 面糊、薄脆、火腿拖入；鸡蛋、香葱点击；摊面和抹酱保持原划动规则。槽位显示对应操作提示，并按步骤、库存和补货状态高亮。
- 豆浆杯显示框为 102×110，袋装煎饼为 158×116；碗使用 228×136 专用框，食材托盘使用 280×128 框，均等比适配。盘内食材区域由 180×66 增至 232×96，补货按钮移至食材下方的右侧，名称、操作提示和数量收拢在器物底部。生油条盘为 240×128，熟油条架为 240×142。油条、成品及豆浆的拖拽显示同步调整。
- 成品和豆浆托盘按单份食物收紧，垃圾桶降为次要视觉层级；成品取用热区覆盖袋子高于托盘的部分，提示位于袋子右侧，并处理了顾客与成品的显示层级。
- 装袋后使用 0.22 秒移动表现进入炉边固定托盘，期间不能取餐。动画由工作台 Tick 推进，与营业一同暂停；减少动态效果时立即到位。转移与到位阶段不在炉面重复显示成品。
- 成品托盘一直保留；装袋成品存在时隐藏空托盘文字，避免成品遮挡标签。交付或丢弃后才能制作下一张，不新增成品容量。
- 成品、熟油条和豆浆直接拖给顾客，保持唯一 ID 绑定、提交时复查、无效投放回弹和取消输入规则。
- 订单卡分为状态、商品、配料、耐心条四层，数量放在商品名称下方，配料可换行。耐心条独立零内边距样式，设计高度 8。
- 短时反馈显示在 HUD 下方，忽略鼠标输入，不覆盖顾客交付区域。

此文更新《天津工作台交互优化方案》中“成品、豆浆和垃圾桶位置保留”的旧布局结论；制作分工和交付规则继续有效。

## 布局来源

`Scripts/UI/TianjinWorkbenchLayout.cs` 集中保存天津营业页面的 1920×1080 坐标和槽位规格。下表已同步本次视觉协调性调整。

| 部件 | 设计矩形 / 间距 |
| --- | --- |
| 炉边成品槽（含标牌） | (1076, 568, 250, 110)，托盘图片保持 250×86 |
| 豆浆槽（含标牌和补货） | (1376, 568, 330, 110)，杯盘图片保持 246×86 |
| 配料区域 | (1032, 680, 868, 308) |
| 配料槽 | 每槽 280×152，列间隔 14；两排起点 y680 / y836；浅盘显示宽 248 |
| 丢弃区 | (1754, 534, 146, 120)，位于右上独立区域 |
| 设备整体偏移 | (0, 50)，天津营业页面专用 |
| 生油条槽 / 熟油条槽 | (350, 579, 240, 174) / (350, 773, 240, 184) |
| 炉边状态栏 / 有操作时的按钮 | (606, 925, 410, 56) / (876, 925, 140, 56) |
| 补货按钮 | 至少 66×56 |

## 自动验证（前次布局修复）

`dotnet build --no-restore`：0 警告、0 错误。

| 回归场景 | 通过 | 失败 |
| --- | ---: | ---: |
| StageFourSelfTest | 1592 | 0 |
| StageTwoSelfTest | 136 | 0 |
| StageThreeSelfTest | 116 | 0 |
| WuhanSelfTest | 30 | 0 |
| 合计 | 1874 | 0 |

新增验证覆盖：三级设备与输入区域同步平移且尺寸不变、前移后的生油条实际投篮、炉边按钮与提示不重叠且不越过桌沿、按钮实际翻面、食材位于容器内、固定空位、取料与补货区域分离、垃圾桶区域隔离、薄脆和火腿真实拖拽事件、左右两端连续交付、转移中禁止取餐、暂停和失焦、取消及重开、减少动态效果、成品丢弃。

薄脆和火腿的两个取料热区中心到各版本炉面目标中心的直线距离合计：Lv1 为 1734→1262，Lv2 为 1735→1262，Lv3 为 1740→1265 设计像素，比原始工作台缩短约 27%。这是几何路径指标，不等同于玩家操作耗时。

回归测试的异步等待显式释放 SceneTreeTimer。本轮 StageFour 的断言全部通过，但 headless 退出时仍报告 5 个音频对象（AudioStreamWAV / AudioStreamPlaybackWAV）释放告警；详见验收日志，不能视为无告警运行。StageThree 有测试主动构造损坏存档的预期错误输出。

## 复现

从项目根目录运行；截图场景使用临时测试存档，默认输出到 `.godot`。

```powershell
dotnet build --no-restore
$env:APPDATA = Join-Path (Get-Location) '.godot/enlarged-userdata'
New-Item -ItemType Directory -Path $env:APPDATA -Force | Out-Null
& "D:\Godot\GodotSharp\Godot_v4.7.1-stable_mono_win64_console.exe" --headless --path . --log-file .godot/stage4.log res://Scenes/Tests/StageFourSelfTest.tscn

# GUI 捕获须使用 Hidden 启动，场景完成后自行退出。
$captureProcess = Start-Process -FilePath "D:\Godot\GodotSharp\Godot_v4.7.1-stable_mono_win64_console.exe" -ArgumentList '--path . --log-file .godot/capture.log res://Scenes/Tests/VisualCapture.tscn -- --capture-day11 --capture-bagged' -WindowStyle Hidden -PassThru
$captureProcess.WaitForExit()
```

可组合参数：`--capture-720`、`--capture-bagged`、`--capture-refilling`、`--capture-low-stock`、`--capture-partial-order`、`--capture-pause`、`--capture-workbench-level=2`（或 3）；使用 `--capture-output=绝对路径` 指定输出文件。

视觉验收采用 Godot 实际渲染的 Day 11 五顾客截图，覆盖 1080p、720p、装袋、补货、低库存、部分交付、暂停和三级设备。鼠标流程通过 Godot 输入事件测试；尚未进行由玩家主导的连续营业手感评测。

## 2026-09-08 视觉协调性调整

按标签、右侧比例、全页层级的顺序完成以下调整：

- 配料名称和精确库存放入统一奶油小标牌；常驻的点击、拖入、划动文字改为悬停说明，炉边继续显示当前操作。标牌忽略鼠标，不遮挡交互。
- 豆浆标牌回到杯盘前沿，补货按钮独立在盘右侧；成品托盘的同一标牌在装袋后切换为直接交付提示。
- 四只浅盘显示宽度从 280 收到 248；食材取走不重排、不缩放。后续小料修正已将错位叠放替换为互不遮挡的固定槽位，具体疏密分档见《天津库存与订单气泡优化》。两碗与四盘的标牌按同一行基线排列，上方出餐区留出间隔。
- 面糊等槽位的矩形外框改为标牌状态强调；库存数值保持深棕，低库存、空和补货的状态色放在标牌边缘及补货按钮。
- 生熟油条使用同样的小标牌，炸锅状态降低视觉权重。
- 顶栏分为日期主题、营业进度与时间、收入及暂停三组。订单最小高度 152、内容最小高度 140，放大商品图和数量，统一角标基线。
- 仅在背景材质中轻度弱化上半店铺装饰，台面以下保持原色；不修改原始美术文件。

本轮构建使用 Godot 随附 NuGet 包及现有本地 .NET 引用包完成离线还原：0 警告、0 错误。临时离线构建入口为 `.tmp/tianjin-visual-polish/build-offline.ps1`；后续增量编译附 `-NoRestore`。

| 本轮回归场景 | 通过 | 失败 |
| --- | ---: | ---: |
| StageFourSelfTest | 1829 | 0 |
| StageTwoSelfTest | 136 | 0 |
| StageThreeSelfTest | 116 | 0 |
| 合计 | 2081 | 0 |

主回归包括三档设备、盘沿安全范围、独立补货热区、标牌归属、库存取走不重排、取消拖拽、部分交付、装袋转移、暂停和失焦。日志保存在 `.tmp/tianjin-visual-polish/`；沙箱中的根证书读取提示，以及 StageThree 主动构造的损坏存档错误，不计为无告警运行。

Godot 实际截图已检查：Day11 五顾客三级设备的 1080p/720p、低库存、空库存、补货中、装袋成品，以及 Day15 三商品订单与部分交付。最终截图为同目录的 `final-*.png`。这次完成的是渲染与自动输入回归，尚未开展玩家连续营业的手感评测。
