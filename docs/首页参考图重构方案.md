# 首页参考图重构方案

确认日期：2026-09-14。

## 状态与实施前提

本次交付重构方案和素材清单，不绘制素材、不修改首页；素材齐全后再实施。本文描述待实现目标，不代表当前游戏已经完成重构。

已检查 `resource/art/Global/StartPage`、资源文件名及 `StartPage.zip` 内文件清单，下列 5 张专用素材尚未找到。压缩包中的通用主次按钮不能视为已补齐的首页专用底板。

## 首页布局与行为

- 沿用附件的暖色早餐铺构图：左上大 Logo、中央墙挂地图、底部三个旅程入口。
- 画布沿用 1920×1080 等比缩放，宽屏留边，保证完整展示。
- Logo 从顶部中央移至左上，视觉宽度约 560px，与地图左上边框形成叠放。
- 墙挂地图扩大至约 1080×640px，放在中央墙面；去掉现有地图下方宣传语。
- 地图突出所有已解锁城市，显示图标和城市名；当前城市保留轻微呼吸效果，已完成城市保留完成标记。未解锁城市不显示醒目标记；无有效存档时展示起点天津。五城采用分散的路线示意位置，避免名称重叠。
- 底部从左至右为“继续旅程”“新的旅程”“世界地图”，前两个约 500×150px，地图入口约 310×110px。分别搭配旅行巴士、旅行手账、折叠地图图标。
- 右上角为“设置、帮助、退出”三个圆形按钮，下方显示文字。移除首页独立声音入口，整体静音使用设置内已有的“全部静音”功能。
- 继续、新旅程、地图及退出接回现有流程；墙挂地图仍可点击打开世界地图。无有效存档时禁用继续旅程，默认焦点落在新旅程。
- 当前城市与天数移入继续按钮提示，详细进度仍在原旅行手账页展示。

## 待补素材：5 张

以下尺寸为建议导出尺寸。文件放入 `resource/art/Global/StartPage`，采用表中独立名称，不覆盖共享素材。

| 文件名 | 尺寸 | 内容要求 |
|---|---|---|
| `继续旅程旅行巴士.png` | 512×512 | 奶油色与橙色小巴士，车顶行李，粗棕描边；不带按钮和文字 |
| `世界地图入口图标.png` | 384×384 | 展开的折叠地图、红色定位针、绿色陆地；不带文字 |
| `首页主按钮底板.png` | 1200×400 | 两个主入口共用；金黄色圆角厚底、棕色投影，中央留白，匹配附件的简洁轮廓 |
| `首页地图按钮底板.png` | 800×300 | 与主按钮同风格的小号横向底板，不含地图图标和文字 |
| `城市名称牌底板.png` | 384×160 | 参考天津标记下方的棕色圆角牌、奶油色边框；不写城市名，供五城共用 |

统一交付透明 PNG。后续制作遵循项目“绿底生成→扣透明→检查边缘和 alpha”规则；本次不制作。按钮状态由 Godot 实现，无需分别提供悬停、按下图片。

## 复用资源与视觉边界

背景、Logo、地图、木框、旅行手账、城市节点及设置／帮助／退出图标复用现有图片。现有退出图标是“门＋向外箭头”，可直接沿用。

现有背景的灯具和杯子位置、地图装饰、手账封面与附件存在细节差异，保留现有图片，以布局和层级贴近参考图。广州、扬州继续使用已有通用城市节点，不扩展专属美术。

附件仅作为视觉参考，业务行为以本文已确认方案和现有项目规则为准。

## 素材齐全后的实现与验收

- 修改首页布局及首页专用按钮组合；新增资源独立命名，避免替换共享素材影响其他页面。
- 图片按实际可见区域排版，必要时用 `AtlasTexture` 排除透明留白；文字由原生 `Label` 绘制。
- 不新增公共接口或存档字段；复用现有城市解锁、继续位置、完成状态和设置持久化逻辑。
- 保留新旅程覆盖确认、取消与保存失败时保护原进度的行为；浏览地图不改变继续位置。
- 完成 C# 构建及 `Scenes/Tests/StartScreenSelfTest.tscn` 相关检查，覆盖新旧存档、多个城市解锁、静音、三个功能入口和键盘导航。使用隔离存档与设置路径。
- 使用项目指定 Godot .NET 控制台程序，从项目目录运行；headless 检查显式指定可写日志路径。
- 在 1920×1080、1280×720、1600×720 实际视口检查首页，重点核对五城全部解锁时的标记拥挤、按钮遮挡、文字清晰度及点击区域。实际视口验收不能由 headless 逻辑测试替代。
- 其他旅行页面仅验证入口和返回衔接，不重构其视觉，也不重跑无关城市经营验收。

本次仅新增本文档，未运行游戏测试；首页实施与上述验收均待素材齐全后执行。
