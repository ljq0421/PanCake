# 武汉手账热干面成品图

使用内置 image_gen 生成完整成品，先生成绿色背景，再通过背景提取生成带透明 alpha 的 PNG。

成品：`resource/art/Wuhan/热干面完整成品.png`（1254 × 1254，RGBA）。仅用于武汉经营手账；手账按一张图整体缩放。

生成提示词（参考现有热干面空碗图片）：

```text
Create a single complete Wuhan hot dry noodles bowl game illustration. Reference is the cream ceramic bowl with two teal stripes and small foot in the preceding image; keep that bowl style and 3/4 elevated view. Warm hand drawn 2D casual cooking game art, dark brown outlines, simple soft cel shading, matching the user's ledger screenshots. Rich golden sesame-coated noodles generously fill the entire bowl opening, naturally seated inside with correct rim occlusion and contact shading, one coherent finished dish. Plain classic noodles, no beef, no scallions, no chopsticks, no text, no other objects. Centered fully visible bowl with modest margin. Solid pure chroma green #00FF00 background, uniform with no gradient, no background shadow, no green reflections. This green background will subsequently be removed. Create one square image.
```

背景提取提示词：

```text
Background extraction only: remove the solid green background from this complete noodle bowl illustration and replace with actual transparent alpha. Preserve the entire bowl and noodles exactly: shape, colors, teal stripes, outline, framing and scale. Clean antialiased edges without green fringe, no extra shadows, no other alterations. Output transparent PNG.
```

验证：dotnet build --no-restore 成功，0 警告、0 错误；WuhanLedgerSelfTest 截图模式 59 项通过，0 失败；人工检查 Day 7 和 Day 8 截图。PNG 左上角 alpha 为 0。
