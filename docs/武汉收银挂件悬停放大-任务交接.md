# 武汉收银挂件悬停放大：已授权任务

用户要求在新会话直接实现以下三步，而不是只给方案。工作目录：`D:\Project\ProjectCake\project-cake`。

## 已确认目标

1. 从武汉现有背景拆出收银挂件，保留原有外观，制作独立透明图层，包含挂绳、蝴蝶结、钱袋及流苏。
2. 去掉背景中原来的挂件，补齐后方背景，避免放大时出现旧挂件重影。处理豆皮解锁前后两套背景；只处理必要局部，保留原始素材，不重画其他场景内容。
3. 复用天津及共享按钮的悬停反馈：挂件本体以中心等比放大 4%，移出恢复。可见图案与轮廓一致；不是仅放大透明点击区或描边。默认键盘焦点不能提前消耗悬停变化。

用户此前曾选择“武汉挂件暂时保留高亮”，但最新指令明确要求实现上述三步，已覆盖此前暂缓选择。无需重新询问是否同意这些步骤。

## 保持范围

- 保留武汉现有自动入账、飞币、音效、点击查看营业明细并暂停、关闭恢复及存档规则。
- 这是悬停放大任务，不要求额外添加天津的收款摆动。
- 不修改顾客、订单卡、食材、锅具、托盘或西安城内。
- 不顺带改其他城市或界面。工作区存在其他任务的修改，先检查当前源码和差异，不覆盖、回退或提交无关工作。

## 源码与素材线索

- `Scripts/Gameplay/WuhanCashPendant.cs`：`BuildCashPendant()`。目前创建透明 `Button`，挂件本体仍在背景上，`PathContourHighlight.AttachArtwork` 只负责轮廓。
- `Scripts/UI/WuhanArtCatalog.cs`：`WorkbenchBackground(bool doupiUnlocked)`。
- `resource/art/Wuhan/武汉-热干面-v1.png`。
- `resource/art/Wuhan/武汉-热干面-豆皮-蛋液-v2.png`。
- `Scripts/UI/WuhanWorkbenchLayout.cs`、`Scripts/UI/WuhanArtworkContours.cs`：现有位置、轮廓，核实实际文件后复用。
- `Scripts/UI/ButtonHoverFeedback.cs`：共享居中缩放，可将输入控件与实际可见 `Control` 分开传入。普通入口只有鼠标悬停放大；西安保留此前行为。
- `Scripts/UI/TianjinLivingWorkbench.cs`：`BindPendantHover` 展示独立可见图层的实现方式。
- `Scripts/UI/BackgroundArtContour.cs`：现有背景轮廓/遮罩逻辑，可按需参考。

不要假设本说明中的源码行号或工作区状态未变化。跨文件影响分析先确认 CodeGraph 索引，再结合源码核实。

## 素材与验收

- 读取当前上级及项目 `AGENTS.md`，遵守透明素材规则；生成透明背景素材时，按项目要求先生成绿色背景再扣除并检查 alpha 与边缘。使用适用的图像 Skill 和工具，保持挂件现有造型。
- 如果遇到仍未明确且会改变视觉方向的选择，集中询问必要问题；已经授权的三步直接推进。
- `.NET` 构建，并运行相关真实 Godot 专项；Mono 控制台位于 `D:\Godot\GodotSharp`，运行时指定可写 `--log-file`。
- 用隔离测试存档，避免改动玩家存档。已有测试通过给进程设置工作区内的 `APPDATA` 隔离运行数据。
- 检查豆皮解锁前后两种背景下的实际视口：静止、悬停、移出、快速反复移入移出、禁用/暂停恢复，无重影、接缝或残留放大。
- 验证点击明细和付款飞币仍正常，确保输入区域与挂件位置吻合。
- `Scripts/Tests/ButtonHoverSelfTest.cs` 含此前“武汉挂件没有 ButtonHoverFeedback”的断言。这个断言因最新授权已过时，应更新为验证武汉挂件放大与复位，不要为保留旧断言而跳过实现。
- 现有测试入口/截图可参考 `Scenes/Tests/ButtonHoverSelfTest.tscn`、`artifacts/button-hover/`。只运行本任务相关检查；视觉验收不能仅靠 headless。

交付时说明实际改动、验证结果，并展示武汉挂件静止与悬停效果；不要将未完成或仅修改点击区的实现宣称为完成。
