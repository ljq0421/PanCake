# 武汉牛肉后加与订单排序

2026-09-12。

- 热干面牛肉仅可在拌匀后加入；辣油、葱花仍在开始拌面前加入。加牛肉后保持成品状态，不再搅拌。提前或重复点击不加料、不播放成功动画；正常加料动画期间维持交付锁定。
- 订单气泡按辣油、葱花、牛肉从左至右排列，只显示配方所需小料。排序在展示层完成，配方内容、食材 ID、存档和其他城市顺序不变。
- 更新牛肉解锁教学、悬停说明、失败反馈，以及自动营业和截图用的制作顺序。

## 验证

- `dotnet build --no-restore`：0 警告、0 错误。
- WuhanSelfTest 146、WuhanAnimationSelfTest 135、WuhanWorkbenchSelfTest 742、OrderBubbleSelfTest 322、WuhanGestureSelfTest 548、WuhanMainDeliverySelfTest 18、WuhanDeliverySelfTest 125，共 2036 项通过，0 失败。后面三组以 720p 参数运行；订单测试覆盖 1080p、720p，牛肉动画测试覆盖减少动态效果开关。
- 额外运行订单截图验收：334 项通过；检查两种分辨率的图标排序和边界。截图位于 `.tmp/order-bubbles/wuhan-1920-initial.png`、`.tmp/order-bubbles/wuhan-1280-initial.png`。
- 日志位于 `.tmp/wuhan-topping-*.log`。沙箱有默认用户存档读取、系统证书读取报错，部分场景退出有 ObjectDB 资源警告；各项断言均通过。首次四组使用相对日志路径出现 `user://D:` 路径错误，后续改为工作区绝对日志路径。

## 蛋液替换已接入

使用用户提供的 `武汉-热干面-豆皮-蛋液-v1.png` 替换豆皮解锁后背景，蛋液表面已在背景中，不再叠加整颗鸡蛋。未解锁豆皮的背景仍沿用原图。

`蛋液勺.png` 原始白底图保留，经用户确认仅去白底，另存 `蛋液勺-透明-v1.png`；未调用图片生成或重绘。可通过 `tools/prepare_wuhan_egg_ladle.ps1` 重复提取，只移除与画布边缘连通的白底，保留金属和蛋液内部高光。

静置勺子位于蛋液托盘内，点击原蛋液热区播放舀取、移到锅面、倾倒、回位及自动摊开。动作沿用原 0.6 秒时长、状态判定、重复点击保护、暂停冻结和火候时钟；减少动态效果模式保留静置勺与锅内渐显，不进行横跨工作台的移动。教学及悬停文字改为蛋液容器。

本次验证：构建 0 警告、0 错误；动画自测 138 项、手势自测 548 项均通过。1080p 普通动画和 720p 减少动态效果的三级设备截图全部完成，检查静置、倒入和摊开终态。截图在 `.tmp/wuhan-animations/1080-normal/` 与 `.tmp/wuhan-animations/720-reduced/`。额外金币回归中武汉分支通过，随后天津分支的“五位顾客气泡不遮挡挂件”断言失败，未改动该分支。日志仍有系统证书读取和既有背景 UID 回退警告。
