# P1 完整煎饼垂直切片实现与验证报告

> 日期：2026-08-01  
> 引擎：Godot 4.7.1 stable  
> 渲染器：Forward Mobile / D3D12

## 阶段结论

P1 代码与自动验证已完成：当前主场景可以从顾客订单开始，依次完成自动倒面、连续摊面、确认面饼、拖入完整鸡蛋、用 T 形摊面器连续摊蛋、双面火候与翻面、双酱料、四种配料拖放、连续折叠、三种包装、出餐评价、收款和下一订单。

该结论不等同于产品验收通过。开发计划要求的 P0 连续制作 20 张综合试玩仍未完成，P1 真人验收单也仍待执行；本次依据用户明确指令提前实施 P1，不反向改写原阶段门槛。

## 数据与状态

- `PancakeModel` 保留 P0 覆盖、厚度、湿润、破损和甜面酱字段，新增独立第二面熟度、辣椒酱浓度、翻面状态，以及蛋白、蛋黄和鸡蛋成熟度网格。
- 完整鸡蛋落到完整饼皮后生成液态鸡蛋层；首次有效 T 形摊蛋样本打散蛋黄，之后按距离采样、T 形横杆接触范围和受热凝固程度转移蛋液。渲染、翻面门槛和评分读取同一组字段。
- 火力按固定模拟步推进当前接触面的局部熟度；厚区升温较慢，边缘拥有轻微加速，两面数据独立评分。
- `IngredientModel` 独立保存鸡蛋、薄脆、火腿肠和葱花的类型、位置、方向、结构负载与湿度，不从屏幕像素推断业务结果。
- `OrderService` 提供 4 个确定性订单组合；同一名 P1 顾客循环使用这些组合，订单卡给出明确配料、酱料和火候要求。
- `P1Session` 管理摊面、第一面、第二面、刷酱配料、折叠、包装、出餐和评价阶段，并保存制作时间、耐心和火力。
- `PancakeFoldModel` 继续读取面饼厚度、火候与破损，同时读取折片内配料结构负载；厚重配料可产生鼓包结果。

## 玩家路径

1. 顾客出现，订单卡显示可验证要求，耐心开始下降。
2. 点击面糊勺自动倒面并进入 T 形摊饼器；完成后点击“确认面饼”。
3. 从左侧托盘把完整鸡蛋拖到完整饼皮上；系统自动拿起 T 形摊面器。连续画圈打散蛋黄并扩大蛋液覆盖，达到最低覆盖和第一面熟度后才能翻面。
4. 调整火力并观察第二面；达到最低熟度后确认火候。
5. 按住右上两个背景酱瓶之一蘸取对应酱料，使用原连续刷酱路径；两种酱料拥有独立浓度场和正式纹理。
6. 从左侧托盘拖入薄脆、火腿肠和葱花；落在鏊外、无饼区域或破洞上会拒绝并给出原因。
7. 开始折叠后锁定前序制作；正常折叠可用纸袋，轻度失败可用纸套，严重失败可改用托盘。
8. 出餐后显示八项分数、标签和顾客短句；收款后重置所有单张状态并进入下一订单。

## 场景与表现

- 顾客、耐心条、订单卡、左侧配料盘、右侧工具盘、双酱瓶热区、火力与阶段控制、评价面板均为 `workstation.tscn` 稳定节点。
- 新控制条放在左侧六格托盘的空第三行，没有覆盖中央实际椭圆操作域。
- 配料层是稳定场景节点，具体 Sprite 根据业务模型中的放置记录生成；鸡蛋例外地使用稳定 `EggCrackArtwork` 表现刚打入瞬间，首次摊动后切换为程序化蛋液层。
- Shader 继续复用原 RGBA8 字段纹理、破损 R8、甜酱 R8 和辣酱 R8，并新增可复用的鸡蛋 RGBA8 字段纹理；蛋白、蛋黄、成熟度和覆盖共同决定颜色与透明度。
- 使用运行时生成的短提示音覆盖倒面、摊面、受热、翻面、刷酱、折叠和出餐，不依赖未记录来源的外部音频。

## 评分

评价读取同一业务数据，输出：

- 面饼完整度。
- 厚度均匀度。
- 两面火候。
- 摊蛋覆盖率与均匀度。
- 订单所需酱料覆盖与浓度。
- 配料正确性和分布。
- 折叠结构与包装代价。
- 订单正确性。
- 制作时间。

评分会区分鸡蛋覆盖不足、局部堆积、摊蛋均匀、缺少配料、多放配料、缺少所需酱料、厚薄不均、两面火候偏差、配料靠边或堆在中央。评价面板新增独立“摊蛋”分。自动分数用于验证数据路径，不代表数值已经通过真人标定。

## 自动验证

- `tests/unit/p1_vertical_slice_self_check.gd`：覆盖鸡蛋液态字段、蛋黄打散、摊蛋覆盖扩张、质量改变总分、双面火候、双酱独立、4 种订单组合、完整状态迁移、翻面门控、第二面门控、正常纸袋、漏料扣分、配料负载导致鼓包，以及纸套/托盘修补标签与 90/55 分结构上限。
- `tests/integration/p1_interaction_self_check.gd`：从真实主场景验证暂停冻结计时与耐心、完整鸡蛋拖放、落蛋图、自动选中 T 形摊面器、翻面前摊蛋门槛、鸡蛋字段纹理、辣酱瓶长按、翻面、折叠、包装、出餐评价、收款与下一订单。
- `tools/run_checks.ps1`：包含全部 P0/M0 回归和新增 P1 检查。
- `tools/run_p1_smoke.ps1`：Godot 4.7.1 Forward Mobile 实际渲染，生成接单、制作和评价截图。

最新 P1 冒烟结果：120 个稳定渲染帧 P95 为 16.83 ms。该结果只代表本机 NVIDIA GeForce RTX 5070 / D3D12 环境，不是其他目标 PC 的性能保证。

## 验证产物

- 接单：`tmp/validation/p1_order_latest.png`
- 摊蛋进行中：`tmp/validation/p1_egg_spread_latest.png`
- 制作：`tmp/validation/p1_production_latest.png`
- 评价：`tmp/validation/p1_result_latest.png`
- 真人验收：`tests/manual/p1_vertical_slice_playtest.md`

## 仍需真人确认

- 首次游玩是否能在有限提示下完成订单。
- 第一面与第二面火候是否容易判断，当前最低翻面/完成阈值是否合适。
- 四种配料的真实拖放落点、方向和视觉尺寸是否顺手。
- 双酱瓶透明热区、酱料切换清空刷子余量的规则是否直观。
- 正常订单是否能稳定落在 35–70 秒，耐心压力是否合理。
- 九维评分（含摊蛋）、标签和顾客反馈是否与玩家实际观察一致。
- 程序化提示音的频率、音量和辨识度。
- P0 连续制作 20 张综合试玩及 10 分钟连续操作疲劳门槛。
