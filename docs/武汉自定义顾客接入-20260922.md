# 武汉自定义顾客接入（2026-09-22）

用户确认：武汉保留通用顾客，加入现有 10 位武汉顾客，排除天津专属人物；正式版与试玩版共用。

## 出场规则

- 普通池：10 位通用人物＋10 位武汉人物。
- 通用人物：年轻女性、普通男／女上班族、老大爷熟客、晨练大爷／阿姨、学生、外卖骑手、出租车司机、外地游客。
- 上班族池加入光谷女工程师、武汉高校男学生、轮渡工作人员、青山产业工人。
- 熟客池加入老汉口婆婆、东湖骑行女青年、汉绣手艺人、汉剧演员。
- 大单池加入汉正街服装店女老板、昙华林文创店女店主；游客池沿用外地游客。
- 复用原有种子选择、同屏外观避重和五人槽位机制。不改变订单生成、耐心、收入或存档结构；图鉴中的人物口味和故事没有额外实现为玩法。
- 天津与西安原候选池保持原样。

## 素材与显示

原始立绘和表情图保留。沿用天津准备脚本，每人生成身体一张与开心、正常、不耐烦、生气四张头部，共 50 张 PNG，位于 `resource/art/Wuhan/Customers`。

`tools/wuhan_customer_sources.json` 记录源图及表情区域；`tools/wuhan_portrait_calibration.json` 保存逐人颈部锚点、头宽、身体切分线和腰线。复现：

```powershell
./tools/prepare_wuhan_customer_portraits.ps1
```

武汉使用自身的画像资源映射，通用顾客复用已有资源。柜台显示沿用天津的正常头像面积归一及固定头身变换，所有表情共用比例和位置；武汉工作台、槽位和订单卡布局保持原有结构。

## 验证

- `dotnet build --no-restore`：0 警告、0 错误。
- `WuhanCustomersSelfTest`：正式与试玩模式通过；覆盖候选池边界、所有人物可被选中、避重、50 张资源加载、表情边界和尺寸、真实武汉五人同屏。
- `StageFourSelfTest --customer-art-only`：213 项通过。
- `XianSelfTest`：138 项通过。
- 实际视口：1920×1080 的 10 人×4 表情，以及 1280×720 正常表情和新旧人物混合出场。截图与日志位于 `output/wuhan-customers`，最终截图以 `final-` 开头。

天津完整 `StageFourSelfTest` 另运行一次：5819 项通过、25 项失败。失败涉及旧升级总价（断言 1140，当前 1080）、解锁日程、工作台输入及旧的直接子节点查找；末项停在 `TianjinWorkbenchSelfTest.cs` 的 `GetChildren().OfType<TextureRect>().Single(...)`，当前画像图层已位于 ArrivalMotion 子节点下。本次未修改这些无关业务规则，也未将综合测试报告为通过。与本次共享目录变更直接相关的顾客资源检查已单独通过。
