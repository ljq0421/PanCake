# 天津教学提示合并与就近定位

2026-09-17 用户确认：合并教学面板，提示挨着当前操作位置。

- 天津教学标题、当前步骤及“跳过教学”共用一张卡片；练习期间不再另显示“本次关闭”面板。
- 完成后沿用同一张卡片显示“开始营业”；保存失败保留错误说明及“重试保存”。
- 天津操作提示依据真实控件或素材轮廓，在目标上、下、左、右选择邻近位置，并限制在视口内，避让当前操作目标及进度显示。
- 普通营业中的首次操作提示同样就近显示，保留本次关闭功能。武汉未启用此次布局调整。
- 教学保护、学习记录、收入、存档和跳过流程沿用现有机制。

## 验证

- `dotnet build --no-restore`。
- `DemoTutorialSelfTest.tscn -- --demo-pilot --capture`：104 项通过，含合并面板、目标避让、邻近距离、跳过、完成及保存失败重试。
- 同专项增加 `--small --english`：105 项通过，实际 1280×720 视口检查英文长文案。
- `TutorialFocusSelfTest.tscn -- --capture`：138 项通过，覆盖天津维护教学与武汉共享逻辑。隐藏图形窗口下曾出现一次武汉首步检查失败；headless 113 项通过，补充状态诊断后的实际视口复跑也全部通过，诊断代码已移除。
- 画面检查：`.tmp/tutorial-panel/1080/`、`.tmp/tutorial-panel/720/`。取料、摊饼、翻面、刷酱和交付提示均使用单张就近卡片。
