# 设置页重构与验收（2026-09-16）

## 已实现

- 保留 `BookBounds(240,150,1400,800)`；按后续确认去掉“旅途设置／画面与窗口／声音／语言与辅助”四个标题及其附属星标。左页显示选项与语言，右页声音选项，完成按钮位于右页底部居中。左右设置区顶部对齐，键盘顺序先遍历左页再到右页。
- 设置页专用奶油／暖棕／暖金控件，原生 HSlider、OptionButton 和状态开关。新增六张图标通过 AtlasTexture 排除导出杂边；PNG 原图未修改。
- 书页材质仅在设置页局部弱化次要星星与路线；其他书页共用原素材。
- 窗口模式与分辨率分离；常用档位扩展至 1440p、4K，按当前显示器工作区筛选。无边框全屏跟随桌面并禁用分辨率选择。手动改变的当前窗口尺寸会作为当前值显示。
- 保留 15 秒显示确认与回退；同值选择不弹窗；切回窗口化恢复已确认尺寸。
- 新增垂直同步开关，默认开启，通过引擎读取实际接受的状态，回退时显示说明。
- 全部静音不改动三个音量值；调整滑条不自动解除静音。语言通过下拉菜单选择简体中文或 English。
- 兼容旧配置，新增 `display.window_width`、`display.window_height`、`display.vsync`；保留原有配置键与 Demo 隔离，不改经营存档。

## 代码入口

- `Scripts/UI/StartScreen.Settings.cs`：设置页构建、图标裁切、状态同步及控件输入。
- `Scripts/UI/JournalSettingsTheme.cs`：设置页局部控件主题。
- `Scripts/Core/JourneySettings.cs`：显示事务、分辨率筛选、垂直同步与配置兼容。
- `Scripts/Tests/SettingsPageChecks.cs`：正式版与 Demo 共用的设置专项；现有两个 UI 测试入口新增 `--settings-only`。

## 初版验证结果

- `dotnet build --no-restore`：通过，0 警告、0 错误。
- 正式版实际图形视口：1920×1080、1280×720、1600×720，每次 52 项通过，均输出 `SETTINGS_FULL_SELF_TEST_OK`。
- Demo headless 设置专项：35 项通过，输出 `SETTINGS_DEMO_SELF_TEST_OK`。物理窗口尺寸与显示器行为由上述实际图形运行验证，不在 headless 中模拟通过。
- 覆盖 Tab 顺序、下拉菜单键盘选择与 Esc、滑条方向键及点击、静音保留音量、垂直同步与驱动状态一致、显示主动回退与超时、确认保存、全屏往返及重载、旧配置与写入失败、经营数据不变。
- 实际截图已检查中英文、分辨率菜单、静音、确认弹窗及全屏；英文显示模式按钮不会撑出左页，完成按钮不压书页底边。
- 三次图形运行无引擎错误。Headless 加载现有主场景时仍有 `FoodInk` 的 `custom_samplers` 编译提示，未影响专项断言；本次未改该城市材质。
- 当前显卡为 NVIDIA RTX 5070、Compatibility 渲染器；其他设备的垂直同步拒绝分支未实机覆盖，由设置后的引擎读回处理。

## 后续布局调整验证

去掉四个标题并将语言选项移到左页后，重新构建通过（0 警告、0 错误）；1920×1080 与 1280×720 的实际视口设置专项各通过 52 项。中英文截图已复核，Tab 顺序调整为显示模式、分辨率、垂直同步、语言、三个音量、静音、完成。

## 复现与截图

从项目根目录运行，使用 Godot Mono 控制台程序，日志写入可写的临时目录：

```powershell
& 'D:\Godot\GodotSharp\Godot_v4.7.1-stable_mono_win64_console.exe' `
  --path . --log-file "$env:TEMP\cake-settings-1920.log" `
  res://Scenes/Tests/StartScreenSelfTest.tscn `
  -- --settings-only --capture --journey-settings 'res://.tmp/settings-bootstrap.cfg'
```

附加 `--small` 为 1280×720，`--wide` 为 1600×720。Demo 使用 `Scenes/Tests/DemoUiSelfTest.tscn`，增加 `--demo-pilot`；纯逻辑检查加 `--headless`，不加 `--capture`。

测试配置与进度使用 `.tmp/start-tests/<随机目录>` 或 `.tmp/demo-ui-tests/<随机目录>`，不写玩家进度。

截图位于 `.tmp/start-review/{1920,1280,1600}/`：

- `settings-new-zh.png`、`settings-new-en.png`
- `settings-resolution-menu.png`、`settings-muted.png`
- `settings-display-confirmation.png`、`settings-fullscreen.png`

本次保留工作区中已有及并行发生的导航、帮助页和天津弹窗改动。
