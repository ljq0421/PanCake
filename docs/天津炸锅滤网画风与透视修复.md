# 天津炸锅滤网与生油条描边

日期：2026-09-12。第三轮按用户反馈调整滤网与锅体之间的色差；保留第二轮的锅口透视、升降遮挡和生油条粗描边。

## 当前接入

- `resource/art/TianJin/Workbench/阶段工作台-升降滤篮-v4.png`：网条主体和篮沿改为低饱和暖灰金属色，暗部为炭灰色；棕色保留在轮廓，去掉 v3 偏红棕的木质感。素材为 1950×804 RGBA，网孔和外围均透明。
- `TianjinArtCatalog` 改用 v4；前序版本原图保留。
- 滤网由独立 `EmbeddedBasketMetal` 节点绘制，位于食物之后，材质设置 `detail_strength=0`。共享 `FoodInk` 原先会把中性灰暗部混入最多 65% 棕色，因此单独停用滤网的内部染色；仍保留原有 0.4 屏幕像素外描边。生、熟、焦油条的材质不随滤网配色变化。
- `FryerVisualView.EmbeddedBasketRect` 表示篮口平面，映射到现有锅口，而非把整张含前篮壁的图片塞进锅口。
- 按用户“滤网偏左”反馈，将锅口原图锚点 x 从 150 改为 156，整体右移 6 个原图像素（1920 设计坐标约 6.89 像素），宽高不变。滤网中心由 276.5 对齐实际内胆中心 282.5，篮内食物和升降效果使用同一锚点。
- v3 的前篮沿位于图高约 82%；前篮壁位于其下。篮口映射到锅口全深度后，通过固定前锅沿裁切下沉部分；升篮仍使用 22 设计像素位移，前篮壁随之露出。
- 生油条由独立 `RawFoodInk` 节点绘制，沿用 `FoodInk` 深棕色，外描边改为 1.65 屏幕像素。篮内绘制与整批拖拽预览共用该材质；熟油条、焦油条和气泡保留原材质。

## v4 配色生成记录

使用内置 `image_gen`，以 v3 素材为编辑目标、原炸锅内胆与夹子的暖灰色为参照，仅调整配色。先生成绿底，再用 `tools/key_tianjin_workbench.py` 抠透明；源图保留在 `resource/art/TianJin/Workbench/Sources/阶段工作台-升降滤篮-v4-green.png`。

最终配色提示词：

```text
Use case: precise-object-edit. A STRICT COLOR-ONLY edit of image 1, the isolated game fryer basket. Image 2 is palette reference only: the charcoal warm-gray inner tub and gray tongs on the cream-and-yellow fryer. Change image 1's reddish wooden brown METAL FILL AND SHADING to the same restrained neutral warm-gray family as the fryer inner tub. Basket must read as matching painted gray metal rather than a wooden brown basket. Mesh wire broad midtones should be warm medium gray (#777170 to #928A85), shadows muted charcoal gray (#4D484B), rim fill warm gray (#8A817D) with a restrained lighter warm-gray highlight (#AAA19A). KEEP the thick dark chocolate brown INK OUTLINES of image 1, but limit brown to actual outline, not the entire width of each metal wire. Maintain chunky dark contours and clearly visible gray centers. No bright white chrome, no mirror shine, no wooden reddish-brown broad fill, no gold, no copper. CRITICAL INVARIANTS: keep exactly the same silhouette, camera perspective, canvas proportions, outline thickness, grid hole count, grid spacing, rear wall height, front rim height (at approximately 82% of the tightly cropped basket sprite), mesh shape and every wire position as image 1. Do not redesign, reshape, relight, add, or remove anything. This is recoloring an existing production sprite, preserving line art and alpha silhouette. Solid chroma green #00FF00 background outside AND through every hole, for subsequent keying; same composition with modest green margin. Only ONE empty handleless basket; no fryer, food, tongs, labels or extra objects in output.
```

## v3 透视与描边生成记录

使用内置 `image_gen`。先以原背景的炸锅局部为参照，在锅内重画篮子，再从该画面单独提取、简化滤网。遵循项目规则，先生成纯绿背景，再运行现有 `tools/key_tianjin_workbench.py` 抠图。

绿底源图：`resource/art/TianJin/Workbench/Sources/阶段工作台-升降滤篮-v3-green.png`。Sources 使用 `.gdignore`，不参与游戏导入。

最终一轮提示词：

```text
Use case: precise-object-edit. Use the basket INSIDE this reference fryer as the edit target. Isolate ONLY that empty wire basket onto completely uniform solid chroma green #00FF00, including green THROUGH EVERY wire hole. Remove all fryer body, oil, environment, knob, tongs, surface and shadows outside the basket. Keep EXACT basket perspective and proportions from this image: rear long rim almost as wide as front, left/right side rim lines close to upright, rectangular and with shallow convergence. Do NOT redraw it as a strongly tapered generic basket. Same camera and same geometry. The edit also strengthens its 2D cartoon ink: make all exterior dark chocolate brown outlines FOUR TIMES more visibly substantial relative to the basket; the main exterior contour must be 1.2% of basket width thick, all mesh brown strokes 0.65% of basket width, with very rounded hand-inked edges. Most of the wire should read as DARK BROWN, not pale metal. Remove the light center stripe on each mesh wire entirely. Mesh wires are solid warm dark brown hand-painted strokes with slight soft irregularity. The narrow rounded rim can have a muted taupe flat fill but no white highlights; use one subdued beige stroke along only portions of the rim. Very simple painterly cel shading. Suppress the photorealistic beveled shiny 3D look. Match the bold brown contour style of the fryer body, but output no fryer. Keep mesh roughly 11 columns wide; background pure green visible through all holes and outside. Lower the back wall height to about ONE row of big mesh holes by moving the floor back edge upward, no change to the outside rim shape. Top floor takes up most of visible basket. No food, no handle, no white or ivory bright strips, no oil, no text, no checkerboard. Entire sprite visible, with modest green margin around it.
```

## 第三轮验证（v4）

- 离线构建成功，0 警告、0 错误；本轮不重复运行与配色无关的业务全套自检。
- 使用最终素材和独立金属材质拍摄 7 张营业台截图，覆盖空篮、生油条、炸制、抬篮、沥油、焦糊和两个背景阶段。截图日志未出现脚本、着色器错误。
- RGBA 检查：alpha 范围 0～255，可见绿色溢色像素为 0。
- 结果位于 `.tmp/tianjin-basket-review/v4-final/`，对比图为 `.tmp/tianjin-basket-review/v4-final-comparison.png`。

## 后续水平对齐修正

原图内胆轮廓的横向中心中位数为 282.5，原锚点中心为 276.5，因此仅将 `TianjinWorkbenchLayout.EmbeddedOpening` 的 x 改为 156。宽高、垂直坐标、材质均未改变；滤网、生熟油条和升降效果继续共用锅口矩形。

差异格式检查通过。本次尝试构建被账本代码 `Scripts/UI/BusinessDetailsView.cs:154` 调用未定义的 `RefreshArtRows` 阻断，未修改该独立编辑，因此右移后的实景截图尚未重拍；前面的 v4 截图是右移前的结果。

## 第二轮验证（v3）

- 离线依赖构建成功，0 警告、0 错误。
- 在图形模式运行 `StageFourSelfTest.tscn -- --embedded-workbench-only`，364 项通过、0 项失败。
- 已检查 Day 8 / Day 15 的空篮、装料、油炸、抬篮、沥油、焦糊截图；前后两轮分别保存在 `.tmp/tianjin-basket-review/after/` 和 `.tmp/tianjin-basket-review/v3/`。
- 补查 720p、Lv2、8 根生油条及熟油条，两张截图中的网格、食物轮廓和前锅沿遮挡可读。本轮共 9 张状态截图，最终对比图为 `.tmp/tianjin-basket-review/v3-final-comparison.png`。
- RGBA 检查：alpha 范围 0～255，可见绿色溢色像素为 0。
- 当前图形模式日志仍有运行环境的系统证书读取提示；无脚本或着色器错误。
