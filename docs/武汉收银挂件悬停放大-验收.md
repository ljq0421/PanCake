# 武汉收银挂件悬停放大

2026-09-20，按任务交接中的三项授权实现。

## 实现

- 原始两套背景保留，新增 `武汉-热干面-v1-无挂件.png`、`武汉-热干面-豆皮-蛋液-v2-无挂件.png`。仅替换右侧挂件及投影所占的墙面局部，其他位置像素不变。
- 新增 `武汉收银挂件-透明-v1.png`，包含挂环、挂绳、蝴蝶结、钱袋、金币和流苏。使用 imagegen 生成绿底拆层参考及补景，最终拆层保留原图像素，经绿底中间图扣除背景，避免生成过程重采样改变原有细节。
- `WuhanCashPendant.cs` 将透明按钮与实际 `TextureRect` 图层分开，复用 `ButtonHoverFeedback`，鼠标悬停时本体居中等比放大 4%。`ArtContourHighlight` 从透明图层取得轮廓并同步变换。点击范围调整为完整挂件区域。
- 默认键盘焦点不放大；移出、禁用、暂停、失焦后恢复。自动入账、付款飞币、音效、明细与存档代码保持原有逻辑。

## 验证及证据

- .NET 离线构建通过，0 个警告、0 个错误：`artifacts/wuhan-pendant/build.log`。
- 实际 Godot Mono / NVIDIA OpenGL 视口：`CoinCollectionSelfTest.tscn -- --wuhan-only --capture`。覆盖 1920×1080 和 1280×720、豆皮解锁前后、普通及减少动态效果设置。
- 最终结果：`COIN_COLLECTION_TEST_RESULT passed=356 failed=0`。已逐图检查两个阶段、两个分辨率的静止与悬停，以及付款截图，无重影、接缝或残留放大。启动日志有环境证书库读取提示，本次本地视口及专项检查均正常完成。
- 覆盖静止、默认焦点、原生鼠标悬停、固定中心、快速反复移入移出、禁用、暂停与恢复、失焦，以及点击明细、Esc/按钮关闭、飞币冻结与恢复、收入及重复交付回归。
- 测试通过工作区内 `APPDATA` 和独立测试存档隔离玩家数据。旧测试夹具增加账本转场等待，并在转场结束后补上正式游戏逐帧进行的界面刷新；没有改变正式账本行为。
- `ButtonHoverSelfTest` 已将旧的“武汉仅高亮”断言更新为真实图层放大与复位；阶段背景断言更新为新派生素材。
- 素材校验：16,853 个不透明像素与原图相同，20,547 个透明像素；绿边像素及不透明画布边界均为 0。两套背景在局部补景范围以外的差异均为 0。见 `artifacts/wuhan-pendant/asset-verification.log`。
- 实际视口截图与日志均位于 `artifacts/wuhan-pendant/`；`rest-hover-comparison.png` 是 1920×1080 实拍截图的原尺寸局部对照。

原始素材、其他城市及工作区已有的其他任务修改未回退或提交。
